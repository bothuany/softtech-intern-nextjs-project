import LinearProgress from "../UI/API/LinearProgress";
import Box from "../UI/Layout/Box";

const Loading = () => {
  return (
    <Box sx={{ width: "100%" }}>
      <LinearProgress />
    </Box>
  );
};

export default Loading;
